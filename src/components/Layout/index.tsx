import React from "react";
import Header from "./Header";
import { type FC } from "~/utils/language/types";
import Head from "next/head";

export default function Layout({ children }: FC) {
  return (
    <div className="flex min-h-screen flex-col bg-slate-50 font-custom text-base text-gray-600">
      <Head>
        <title>MGDigital</title>
        <meta name="description" content="Generated by create-t3-app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <Header />
      {/* {(router.pathname !== "/" || session) && (
        <div className="absolute -top-16 -z-10 h-[100vh] w-full overflow-hidden rounded-b-[15%] bg-primary-200"></div>
      )} */}
      <main className="relative z-10 flex flex-1 flex-col px-8 pb-4 md:px-20 md:pb-16">
        {children}
      </main>
    </div>
  );
}
